version: 0.2
phases:
  install:
    commands:
      - apt-get install -y git
      - git --version
      - git clone https://github.com/e-conomic/hiring-assignments.git
      - ls -l
  pre_build:
    commands:
      - echo "Login AWS EKS"
  build:
    commands:
      - echo "Building the Docker image"
  post_build:
    commands:
      - echo "Pushing the Docker images"